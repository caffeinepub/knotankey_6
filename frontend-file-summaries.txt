{
  "frontend/src/components/cart/CartItem.tsx": "Cart item row showing product image, title, per-item price and line total (both formatted as ₹ INR via `formatINR`), quantity selector, and remove button; all USD/$ references removed.",
  "frontend/src/components/checkout/OrderSummary.tsx": "Checkout order summary card showing cart items with images, quantities, line totals, subtotal, a dynamic shipping row (₹80 or \"FREE SHIPPING\" based on the ₹2999 threshold), and a grand total that equals subtotal plus shipping — all formatted as ₹ INR via `formatINR`.",
  "frontend/src/pages/ProductDetailPage.tsx": "Product detail page fetching a product by the correct route param name ($id), using the CartContext's addToCart method with the correct CartItem shape (productId, imageUrl), with skeleton loading, error state, quantity selector, and add-to-cart/buy-now actions.",
  "frontend/src/pages/ThankYouPage.tsx": "Order confirmation page with animated reveal, order ID display, a \"Secure Payment Process\" notice box (beige #F8F5F0 background, lock icon) explaining the email payment link flow, next-steps info cards, and navigation links back to home/products.",
  "frontend/src/pages/CustomOrderPage.tsx": "Custom order request page with a full form that uploads inspiration images as ExternalBlob with progress tracking and submits to the backend via useCreateCustomOrder mutation. The Description textarea and Color Preference input are now mandatory with `required` attributes and red asterisk (*) indicators on their labels, joining Product Type, Size, Budget Range, and Email as required fields.",
  "frontend/src/pages/ReturnsPage.tsx": "Returns & Exchanges page with all six form fields required (Order Number, Customer Name, Email, Reason for Return, Message, Video Upload), inline per-field error messages on attempted submission, video upload section titled \"Upload Unboxing / Condition Video\" with MP4/MOV/WEBM validation, 50MB size limit, file preview, and upload progress indicator.",
  "frontend/src/pages/AdminPage.tsx": "Admin panel with passcode gate (checking against hardcoded knotankey_admin_2026), tabbed dashboard passing the passcode prop to all four admin sub-components for authenticated backend access.",
  "frontend/src/components/admin/AdminProductsManagement.tsx": "Admin products panel with full CRUD (create/update/delete), image upload via ExternalBlob, skeleton loading, and toast notifications; the category field is now a free-text `<Input>` (no predefined options, no CATEGORIES array), and all prices display in ₹ INR via `formatINR`.",
  "frontend/src/components/admin/AdminOrdersManagement.tsx": "Admin orders panel fetching all orders via useGetOrders(passcode) and updating order status via useUpdateOrderStatus mutation, with skeleton loading, inline status dropdowns, and toast notifications.",
  "frontend/src/components/admin/AdminCustomOrdersViewer.tsx": "Admin custom orders viewer fetching all custom order requests via useGetCustomOrders(passcode), displaying all fields including inspiration image, with skeleton loading and error states.",
  "frontend/src/components/admin/AdminReturnRequestsViewer.tsx": "Admin return requests viewer displaying all submitted return requests with Order Number, Customer Name, Email, reason badge, date, Message, and an inline HTML5 video player with a Download Video button for each entry; uses ExternalBlob.getDirectURL() for playback and getBytes() for download.",
  "frontend/src/components/layout/MusicToggle.tsx": "Background music toggle button using the Pixabay audio URL, with volume capped at 0.2, no auto-play, loop enabled, a fade-in effect over ~1.5 seconds on play, and Play/Pause icons reflecting current playback state.",
  "frontend/src/components/layout/Navbar.tsx": "Fixed navbar with brand name using Nunito 700 and chocolate brown color, centered desktop nav links, cart button with item count badge, and mobile menu toggle; the MusicToggle component has been removed entirely.",
  "frontend/src/components/home/HeroSection.tsx": "Hero section with parallax background image at `/assets/generated/hero-bg.dim_1920x1080.jpg`, a dark gradient overlay (transparent → rgba(0,0,0,0.25) → rgba(0,0,0,0.45)) for text readability, and all hero text (tagline, \"knotankey\" heading, subtitle) updated to white/light-beige (#FFFFFF / #F8F5F0) with `text-shadow: 0px 2px 8px rgba(0,0,0,0.35)`; layout, spacing, button, and navigation are unchanged.",
  "frontend/src/components/home/AboutSection.tsx": "About section with the crochet lace background image preserved and unblurred, a dark gradient overlay (rgba(0,0,0,0.35) → rgba(0,0,0,0.45) → rgba(0,0,0,0.55)) layered between the image and text, all text in luxury beige-white (#F8F5F0) with text-shadow, paragraph text at #EDE7E0 with opacity 0.95, and hero content at z-index 20 to ensure proper stacking above the overlay.",
  "frontend/index.html": "HTML entry point with Google Fonts preconnect and Nunito 700 stylesheet link added to the `<head>` for the brand name font.",
  "frontend/src/components/layout/Footer.tsx": "Footer component with brand info column, a trimmed Shop column (only \"All Products\" and \"Custom Orders\"), and a Policies & Info column; the newsletter signup column, Shop category links (Accessories, Home Décor, Wearables), and the \"Built with caffeine.ai\" attribution have been removed. The footer retains the divider line, gradient background, social icons, and copyright line.",
  "frontend/src/utils/currency.ts": "New utility module exporting a `formatINR(amount: number): string` function that formats numbers as Indian Rupees using `Intl.NumberFormat` with `en-IN` locale and `INR` currency, producing output like ₹249, ₹1,299, ₹10,999.",
  "frontend/src/components/ProductCard.tsx": "Product card component displaying image, title, category, price (formatted as ₹ INR via `formatINR`), quick-add button, and view link; the price bug is fixed by using `Number(product.price)` directly without any division, so ₹350 stored in the backend displays as ₹350.",
  "frontend/src/components/cart/CartDrawer.tsx": "Slide-in cart drawer showing cart items and total (formatted as ₹ INR via `formatINR`), with checkout and view-cart navigation buttons; all USD/$ references removed.",
  "frontend/src/pages/CartPage.tsx": "Full cart page with item list, per-line totals, and order summary sidebar all displaying prices in ₹ INR via `formatINR`; all USD/$ references removed.",
  "frontend/src/pages/CheckoutPage.tsx": "Checkout page with shipping form that computes subtotal, shipping amount (via `calculateShipping`), and grand total before submitting an Order to the backend with all three fields (`subtotal`, `shippingAmount`, `total`) correctly populated as BigInt values.",
  "frontend/src/hooks/useQueries.ts": "Comprehensive React Query hooks for all backend collections (Products, Orders, CustomOrders, ReturnRequests); useCreateReturnRequest mutation updated to accept the new six-parameter signature (orderNumber, customerName, email, reason, message, video as ExternalBlob) matching the backend's createReturnRequest method.",
  "frontend/src/components/home/BestSellersSection.tsx": "Best sellers section that fetches data via useGetBestSellers hook (calling backend getBestSellers), shows skeleton loading and error states, and renders ProductCard components from live backend data.",
  "frontend/src/pages/ProductsPage.tsx": "Products listing page with a search bar and a Filters button; the Filters panel (toggled by the button) contains a Category dropdown (dynamically derived from unique `product.category` values in fetched products, no preloading) and a Price Range filter — no category UI exists outside the panel. Default category is \"All\" so all products display on load.",
  "frontend/src/components/layout/NewsletterSignup.tsx": "Newsletter signup form that collects an email address and shows a success toast on submission — no longer depends on a backend hook, uses local state only since the newsletter subscription backend method does not exist.",
  "frontend/src/utils/shipping.ts": "New utility module exporting a `calculateShipping(subtotal: number): number` function that returns ₹80 for orders below ₹2999 and ₹0 (FREE) for orders ₹2999 and above, along with exported threshold and cost constants."
}